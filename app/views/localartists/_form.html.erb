<%= form_with(model: @localartist, local: true, url: choose_new_or_edit) do |form| %>

<!--エラー  -->
  <% if @localartist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@localartist.errors.count, "error") %> 件のエラーがあります</h2>
      <ul>
      <% @localartist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<!--  -->

  <div class="image">
    <br />
    <p><%= form.label :写真 %></p>
    <% if @localartist.image %>
      <%= image_tag(@localartist.image.thumb.url) if @localartist.image && @localartist.image.url %>
    <% end %>
      <%= form.file_field :image  %>
  </div>

  <div class="name">
    <%= form.label :アーティスト名 %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="country">
    <%= form.label :国 %>
    <%= form.text_field :country, class:"form-control" %>
  </div>

  <div class="wholikes">
    <%= form.label :tag_list, "このｱｰﾃｨｽﾄが好きな人にｵｽｽﾒ" %><br />
    <ul id="wholikes_list">
      <li>
        <%= form.text_field :tag_list,placeholder:",で区切って下さい", value: @localartist.tag_list.join(","), class:"form-control"  %>
      <li>
    </ul>
  </div>
      <!-- #taggableで追加できるようにする paramsも追加-->

  <div class="comment">
    <%= form.label :コメント %>
    <%= form.text_area :post_comment, placeholder:"自由にコメントしてください", class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "投稿する" %>
  </div>


<% end %>
