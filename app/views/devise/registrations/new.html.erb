<div class="sign_up-image">
  <div class="sign_up-logo">
    <h1>DiscoverLocalMusic</h1>
    <h3>世界中の音楽を探そう</h3>
    <h4>あなたの好きな音楽から世界中のｱｰﾃｨｽﾄ、音楽スポットを探すことができます</h4>
  </div>
</div>

<div class="sign_up-form">
  <h2>アカウント登録</h2>
  <% resource = User.new unless resource %>
  <%= form_for(resource, as: resource_name, url: user_registration_path, html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>


    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name, autofocus: true, class:"form-control",placeholder:"名前を入力"  %>
    </div>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control",placeholder:"XXX@example.com" %>
    </div>

    <div class="field">
      <%= f.label :comment %><br />
      <%= f.text_area :comment, autofocus: true, size: "20x10", class:"form-control",placeholder:"プロフィール用の自己紹介を入力(255文字まで)",id: "input-text" %>
      <p id="counter">0文字</p>
    </div>

    <div class="field">
      <%= f.label :password %>
      <% if @minimum_password_length %>
      <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password", class:"form-control",placeholder:"パスワードを入力" %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control",placeholder:"パスワードを再入力"  %>
    </div>

    <div class="field">
      <%= f.submit t('.sign_up'),class:"btn btn-primary btn-block btn-lg" %>
    </div>
  <% end %>

    <div class="field">
      <a><%= link_to t('alreadyregistered'), new_user_session_path %></a>
    </div>

</div>
